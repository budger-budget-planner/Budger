# Budger frontend — environment variables (Vite: only VITE_-prefixed vars are
# exposed to the browser bundle; these are safe to expose, never put secrets
# here).

# Sentry client DSN (optional) — safe to expose in the frontend bundle.
VITE_SENTRY_DSN=
